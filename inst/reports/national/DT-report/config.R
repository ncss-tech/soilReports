# config.R for simple DT report

# list all csv files in input directory (only takes first path)
#  - be sure to quote multiword column names in headers

# paths <- list.files("input", "csv",
#                     recursive = TRUE,
#                     full.names = TRUE)

# single path
paths <- "C:/path/to/your/csv.csv"